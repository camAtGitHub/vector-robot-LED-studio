function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>Backpack Lights Designer — Phase 1</h1>
      <p>
        Domain modules ready: schema, player (GetCurrentLEDcolor port), triggers.
        Full UI arrives in later phases.
      </p>
    </main>
  );
}

export default App;
